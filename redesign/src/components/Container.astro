---
interface Props {
  class?: string;
  as?: "div" | "section" | "article" | "main" | "header" | "footer";
}

const { class: className = "", as: Tag = "div" } = Astro.props;
---

<Tag class={`mx-auto w-full max-w-[1280px] px-6 md:px-10 lg:px-16 ${className}`}>
  <slot />
</Tag>
